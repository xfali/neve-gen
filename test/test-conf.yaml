swagger:
  enable: true
  docPath: docs/swagdoc
gobatis:
  enable: true

scan:
  databases:
    - enable: true
      driver: neve_dummydb
      host: test.io
      port: 3306
      username: test
      password: test
      dbs:
        - dbname: test
          format: xml
          tables:
          #  - tbl_user